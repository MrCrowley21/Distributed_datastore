services:
  data-store:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        config: config
    ports:
      - "8000:8000"
      - "5000:5000"
  data-store-1:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        config: config_1
    ports:
      - "8001:8001"
      - "5001:5001"

  data-store-2:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        config: config_2
    ports:
      - "8002:8002"
      - "5002:5002"

networks:
  default:
    name: data-store-network